declare module "jquery-binarytransport" {
  // The package mutates the global jQuery transport table.
  // We don't rely on its exported types; it is only imported for side effects.
  const value: unknown;
  export default value;
}

